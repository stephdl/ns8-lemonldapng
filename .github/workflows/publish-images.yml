name: "Publish images"

on:
  push:

permissions:
  packages: write

jobs:
  publish-images:
    if: github.run_number > 1
    uses: KSATDesign/ns8-github-actions/blob/main/.github/workflows/publish-branch.yml@v1
